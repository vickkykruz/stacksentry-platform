# The StackSentry platform depends on the real StackSentry package as its
# scanning engine. This is the same tool published to PyPI — not a fork, not a
# reimplementation. The platform imports it and calls its real scan code.
stacksentry>=1.0.0

# Web framework
Flask>=3.0.0

# Production WSGI server (used on the VPS, not in dev)
gunicorn>=21.0.0

# Background task queue for out-of-band scanning (badges never block on a scan)
celery>=5.3.0
redis>=5.0.0
